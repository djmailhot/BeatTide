<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @song.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :api_id %><br />
    <%= f.text_field :api_id %>
  </div>
  <%= fields_for @album do |alb| %>
  <div class="field">
      <%=alb.label :album_name %><br />
      <%=alb.text_field :name %>
  </div>
   <% end %>
  <%= fields_for @artist do |art| %>
  <div class="field">
      <%=art.label :artist_name %><br />
      <%=art.text_field :name %>
  </div>
   <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
